<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8" />
  <title><%= serverName %> 認証</title>
  <script src="https://hcaptcha.com/1/api.js" async defer></script>
  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #1e1e2f;
      color: white;
      text-align: center;
      padding-top: 80px;
      margin: 0;
    }

    .container {
      display: inline-block;
      background: #2a2a3d;
      padding: 40px 50px;
      border-radius: 16px;
      box-shadow: 0 0 25px rgba(0, 0, 0, 0.4);
      min-width: 340px;
      max-width: 420px;
    }

    h1 {
      background: linear-gradient(90deg, #0055ff, #5500ff);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-size: 2rem;
      margin-bottom: 10px;
    }

    h2 {
      font-weight: normal;
      color: #ccccff;
      margin-bottom: 30px;
    }

    .h-captcha {
      display: inline-block;
      margin: 20px 0;
    }

    button {
      margin-top: 20px;
      padding: 12px 36px;
      background: linear-gradient(90deg, #0055ff, #5500ff);
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: all 0.25s ease;
      font-size: 1rem;
    }

    button:hover {
      transform: translateY(-2px);
      opacity: 0.9;
    }

    footer {
      margin-top: 60px;
      font-size: 0.9rem;
      opacity: 0.6;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= serverName %> 認証</h1>
    <h2>HCaptcha認証を行ってください</h2>

    <form method="POST" action="/verify">
      <div class="h-captcha" data-sitekey="<%= sitekey %>"></div>
      <br />
      <button type="submit">送信</button>
    </form>
  </div>

  <footer>
    powered by Morx Server Bot
  </footer>
</body>
</html>
